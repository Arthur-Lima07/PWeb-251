<!DOCTYPE html>
<html lang="pt-br">

<head>
    <meta charset="UTF-8">
    <title>Editar Ficha</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="/stylesheets/editarFicha.css">
</head>

<body>
    <%- include('partials/navbar') %>

        <div class="container mt-5 text-white">
            <h1 class="mb-4">Editar Ficha de <%= ficha.nome %>
            </h1>

            <form action="/fichas/<%= ficha._id %>/editar" method="POST">
                <div class="mb-3">
                    <label for="nome" class="form-label">Nome:</label>
                    <input type="text" id="nome" name="nome" class="form-control" value="<%= ficha.nome %>" required>
                </div>

                <div class="mb-3">
                    <label for="especie" class="form-label">Espécie:</label>
                    <input type="text" id="raca" name="raca" class="form-control" value="<%= ficha.raca %>">
                </div>

                <div class="mb-3">
                    <label for="nivel" class="form-label">Nível:</label>
                    <input type="number" id="nivel" name="nivel" class="form-control" value="<%= ficha.nivel %>"
                        min="1">
                </div>

                <div class="mb-3">
                    <label for="descricao" class="form-label">Descrição:</label>
                    <textarea id="descricao" name="descricao" class="form-control"
                        rows="3"><%= ficha.descricao %></textarea>
                </div>

                <h4 class="mt-4">Atributos</h4>
                <% for (let attr in ficha.atributos) { %>
                    <div class="mb-3">
                        <label class="form-label">
                            <%= attr.charAt(0).toUpperCase() + attr.slice(1) %>:
                        </label>
                        <input type="number" name="<%= attr %>" value="<%= ficha.atributos[attr] %>"
                            class="form-control" min="0">
                    </div>
                    <% } %>

                        <button type="submit" class="btn btn-primary mt-3">Salvar Alterações</button>
                        <a href="/fichas" class="btn btn-secondary mt-3">Cancelar</a>
            </form>
        </div>

        <%- include('partials/footer') %>
</body>

</html>